[project]
name = "first-ai-agent"
version = "0.1.0"
description = "A Python-based AI coding agent powered by Google's Gemini API"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "google-genai==1.12.1",
    "python-dotenv==1.1.0",
]
